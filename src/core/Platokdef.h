#pragma once

const float PIXELSINMETER = 50.0f;
const float WIDTH = 1024;
const float WIDTH_METER = WIDTH / PIXELSINMETER;
const float HEIGHT = 768;
const float HEIGHT_METER = HEIGHT / PIXELSINMETER;